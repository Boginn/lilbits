import { Wrapper, DividerLine, ContactInfo } from './styles';

const Footer = () => {
  return (
    <Wrapper>
      <DividerLine />
      <ContactInfo></ContactInfo>
    </Wrapper>
  );
};

export default Footer;
