import styled from 'styled-components';

export const Wrapper = styled.header`
  text-align: center;
  height: 250px;
  overflow: hidden;
`;

export const Image = styled.img`
  height: 500px;
  margin-top: -120px;
  cursor: pointer;
`;
